export const disableSerper = `
// TEMPORARY BYPASS SERPER
/*
    let serperContext = ''
    if (mode === 'search') {
      const serperKey = process.env.SERPER_API_KEY
      if (serperKey) {
        try { ... }
      }
    }
*/
`
